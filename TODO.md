Use 'maestro record <path to flow> in the following way:
    1. create a javascript which generates a test-report.html file and embeds flow spec names and flow spec recorded videos into it so you can view how tests ran
    2. create a script to serve it locally on port 2077
    3. automate report sending via email (for multiple receivers)