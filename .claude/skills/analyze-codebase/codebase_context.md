# Codebase Context — maestro-demo

> Generated by the `analyze-codebase` skill on 2026-07-10. Refresh by re-running the skill whenever the framework setup changes significantly. This file is the shared memory for this codebase — future skills should load it instead of re-scanning the repo.

## Purpose & Stack

This repository is a **mobile UI automation framework demo built on Maestro** (mobile-dev-inc), testing the **Wikipedia app** on Android (primary) and iOS (secondary). It is not an app codebase — the apps under test are prebuilt binaries checked into the repo.

- **Test framework:** Maestro CLI (installed via Homebrew or `curl get.maestro.mobile.dev`; invoked from `$HOME/.maestro/bin/maestro`)
- **Tooling/runner layer:** Node.js ESM scripts (`package.json` `"type": "module"`), devDependencies: `@devicecloud.dev/dcd`, `dotenv`, `maestro`, `nodemailer`, `open`
- **App under test (Android):** `org.wikipedia` — APK at `apps/android/wikipedia/wikipedia.apk`
- **App under test (iOS):** `Wikipedia.app` at `apps/ios/wikipedia/Wikipedia.app` (copied in from a local Xcode DerivedData build)
- **Optional local LLM tooling:** `ollama:*` npm scripts (serve/run `dolphin-mistral`/kill), plus `maestro:chat` and an AI report (`report:ai:open`)
- **MCP integration:** project-scoped `.mcp.json` registers the built-in Maestro MCP server (`${HOME}/.maestro/bin/maestro mcp`, STDIO). It exposes device/automation tools to Claude Code: `list_devices`, `start_device`, `launch_app`, `stop_app`, `tap_on`, `input_text`, `back`, `take_screenshot`, `inspect_view_hierarchy`, `run_flow`, `run_flow_files`, `check_flow_syntax`, `cheat_sheet`, `query_docs`. Approve the server when prompted at session start.

## Repository Map

```
.maestro/                    Maestro workspace
  config.yaml                Flow inclusion, tag exclusion, timeout, output dir
  flows/wikipedia/
    main/MainScreen.spec.yaml            Skip onboarding → assert main screen
    conditional/LaunchStepper.spec.yaml  Assert onboarding stepper contents
    conditional/Third Flow.spec.yaml     Intentionally failing demo (asserts "invalid_id")
  src/pages/                 Page-object JS files (selector maps)
    LaunchStepper.page.js
    MainScreen.page.js
.scripts/                    Node/bash lifecycle & reporting tooling
  generate-report.js         Builds HTML report from recordings/
  serve-report.js            Generates + serves report on port 2077
  archive-report.js          Zips report into test-report/
  send-report-email.js       Emails the zip via Gmail/nodemailer
  record-flows.js            Records all flows to recordings/ (drives report pipeline)
  kill_ports.sh              Kills PIDs on ADB/emulator/studio/report ports
  kill_processes.sh          pkill maestro/qemu/emulator/adb/AndroidStudio
  ios/ios_test_runner.sh     End-to-end iOS simulator runner
.github/workflows/           4 manually-triggered Android CI pipelines
apps/android/wikipedia/      wikipedia.apk (binary under test)
apps/ios/wikipedia/          Wikipedia.app bundle (binary under test)
.windsurf/rules/             AI-assistant behavior rules (scan, memory, no-unsolicited-edits)
.env                         Email credentials for the report mailer (gitignored secrets)
test-results/                Maestro HTML output (generated, deletable)
recordings/                  Flow .mp4 recordings (generated, deletable)
test-report/                 Archived report zips (generated, deletable)
```

## Test Architecture

**Flows** are Maestro YAML files named `*.spec.yaml` under `.maestro/flows/<app>/<category>/`. Categories currently: `main/` and `conditional/`. All current flows are tagged `smokeTest`.

**Workspace config** (`.maestro/config.yaml`):
- `flows`: `flows/wikipedia/main/*.spec.yaml` and `flows/wikipedia/conditional/*.spec.yaml`
- `excludeTags`: `util`, `recordTemplate`
- `timeout: 10000`, `testOutputDir: test-results`

**Page Object pattern (key convention):** selectors are not inlined in YAML. Each screen has a JS file in `.maestro/src/pages/` that assigns a selector map onto Maestro's `output` object, e.g. `output.launchStepper = { skipBtn: "org.wikipedia:id/fragment_onboarding_skip_button", ... }` ([LaunchStepper.page.js](.maestro/src/pages/LaunchStepper.page.js)). Flows load them with `runScript: file: "../../../src/pages/<Page>.page.js"` and reference elements as `${output.launchStepper.skipBtn}`. Selector values are Android resource-ids or text/regex patterns (e.g. `".*The Free Encyclopedia.*"`).

**Parameterization:** flows declare `appId: ${appId}`; the value is passed at run time via `-e appId=org.wikipedia` (see `maestro:test:wikipedia` script and all CI workflows).

**Conditional execution:** onboarding handling uses `runFlow: when: visible: id: ${output.launchStepper.skipBtn}` so flows work whether or not the onboarding stepper appears (`MainScreen.spec.yaml`, `LaunchStepper.spec.yaml`).

**Demo failure:** `Third Flow.spec.yaml` asserts a literal `"invalid_id"` — it exists to demonstrate a failing test in reports; do not "fix" it.

## NPM Scripts Reference

Environment lifecycle (recommended cycle: cleanup → setup → test → teardown):

| Script | What it does |
|---|---|
| `env:cleanup` | teardown + kill ports/processes/emulators + delete artifacts |
| `env:setup` | start Android emulator (`maestro start-device`, API 34, en_US) + install APK |
| `env:teardown` | uninstall app + stop emulator |

Test execution & authoring:

| Script | What it does |
|---|---|
| `maestro:test:wikipedia` | `maestro test .maestro --config .maestro/config.yaml -e appId=org.wikipedia --format html --output test-results/wikipedia.html` |
| `maestro:inspect` | Maestro Studio (element inspector, port 9999) |
| `maestro:record:wikipedia` | Record LaunchStepper flow to `recordings/LaunchStepper.mp4` |
| `record:all` | Delete recordings, then `node .scripts/record-flows.js` (records every flow) |
| `maestro:chat` | `maestro chat` (AI assistant) |
| `test:ios` | Runs `.scripts/ios/ios_test_runner.sh` |

Device/app management: `maestro:device:start:android`, `maestro:device:stop` (`adb emu kill`), `android:install:wikipedia` (`adb install -t -r -g`), `android:uninstall:wikipedia`.

Cleanup/kill: `kill:ports` (`.scripts/kill_ports.sh` — ports 5037, 5554–5561, 5584, 5585, 9999, 2077), `kill:processes` (`.scripts/kill_processes.sh` — pkill maestro, qemu-system, emulator, adb, Android Emulator, AndroidStudio, studio), `kill:emulators` (adb emu kill per device).

Reports & artifacts: `report:generate`, `report:serve` (port 2077), `report:open`, `report:ai:open` (opens `test-results/*/ai-report-*.spec.html`), `report:delete`, `report:archive`, `report:email`, `send:report:emails` (archive + email), `recordings:delete`, `artifacts:delete`.

Logs: `capture:android:logs` (adb logcat → `android_emulator_logs.txt`), `capture:android:logs:stop`.

Local LLM: `ollama:serve`, `ollama:run` (dolphin-mistral), `ollama:kill`.

## Reporting Pipeline

1. `record-flows.js` runs flows via Maestro and saves `.mp4` recordings to `recordings/`, named `{flowName}-passed.mp4` / `{flowName}-failed.mp4` (status suffix convention).
2. `generate-report.js` reads `recordings/`, derives pass/fail from filename suffixes, and builds an HTML report at `test-results/test-report.html`.
3. `serve-report.js` regenerates and serves the report on **port 2077** (kills the port first).
4. `archive-report.js` zips `recordings/` + `test-results/` into `test-report/`.
5. `send-report-email.js` emails the newest zip in `test-report/` via Gmail (nodemailer), timestamped `DD-MM-YYYY_HH:MM`.

Required `.env` variables (names only — values are secrets): `EMAIL_USER`, `EMAIL_PASS`, `MANAGER_EMAIL`, `LEAD_EMAIL`. Recipients are manager + lead.

Maestro's own HTML result additionally lands at `test-results/wikipedia.html` (from `maestro:test:wikipedia`); AI reports appear as `test-results/*/ai-report-*.spec.html`.

## CI/CD Workflows

All four workflows are **manual (`workflow_dispatch`) only**, Android only, `ubuntu-latest`:

- **android-tests-no-cloud.yaml** — local headless emulator run: enables KVM, Temurin Java 21, installs Maestro CLI, then `reactivecircus/android-emulator-runner@v2` (API 34, x86_64, google_apis, no-window/swiftshader) runs the tests.
- **android-tests-no-cloud-report.yaml** — same emulator setup (with `lfs: true` checkout) but runs the full reporting pipeline: builds `.env` from GitHub secrets, `npm ci`, `record:all` → `report:generate` → `archive-report.js` → `send-report-email.js`, uploads `test-results` as an artifact, and publishes recordings/reports to the **gh-pages** branch via `peaceiris/actions-gh-pages@v4`.
- **android-tests-maestro-cloud.yaml** — `mobile-dev-inc/action-maestro-cloud@v2`: uploads the APK + `.maestro` workspace to Maestro Cloud (secrets `MAESTRO_API_KEY`, `MAESTRO_PROJECT_ID`), API 34, en_US, `exclude-tags: util,recordTemplate`, timeout 60000.
- **android-tests-dcd-cloud.yaml** — `devicecloud-dev/device-cloud-for-maestro@v1` (secret `DCD_API_KEY`): pixel-6, API 34, `include-tags: smokeTest`, `exclude-tags: util,recordTemplate`, retry 1, HTML report, downloads ALL artifacts.

## iOS Testing

`.scripts/ios/ios_test_runner.sh` (run via `npm run test:ios`):
- Simulator: **iPhone 16 Pro**; boots it, installs `apps/ios/wikipedia/Wikipedia.app`, runs Maestro flows from `.maestro/flows/wikipedia/main/`, then shuts down simulators and kills the Simulator app on exit.
- ⚠️ The `.app` is copied from a **hardcoded machine-specific Xcode DerivedData path** (`/Users/oleksandrzavhorodnii/Library/Developer/Xcode/DerivedData/Wikipedia-.../Debug-iphonesimulator/Wikipedia.app`) — this only works on the original author's machine unless the path is updated.

## Conventions & Gotchas

- **Run cycle:** always `env:cleanup` → `env:setup` → `maestro:test:wikipedia` → `env:teardown`. Cleanup before each run avoids stale ports/processes/emulators (README "IMPORTANT" notes).
- **Tags:** `smokeTest` on all real flows; `util` and `recordTemplate` are reserved exclusion tags (excluded in config and every cloud CI run).
- **Known issue:** "App not installed" error when Maestro Studio is open — Studio holds the same emulator; close Studio and re-run (README Troubleshooting, upstream issue mobile-dev-inc/Maestro#1104).
- **`Third Flow.spec.yaml` fails on purpose** (asserts `invalid_id`) — a demo of failure reporting.
- **Selectors live only in page objects** (`.maestro/src/pages/*.page.js`); when adding a flow, create/extend a page file and load it with `runScript` rather than hardcoding ids in YAML.
- **Generated dirs** (`test-results/`, `recordings/`, `test-report/`, `android_emulator_logs.txt`) are disposable artifacts — safe to delete via the `*:delete` scripts.
- `.windsurf/rules/` contains AI-assistant policies for this repo: scan the codebase before answering, consult official Maestro docs for framework questions, persist codebase memory, and **never modify files unless explicitly asked**. These reflect the owner's expectations for AI tooling here.
- Scripts assume **macOS/zsh + Homebrew Maestro install** locally (Windows-specific commands were deliberately removed; see git history).
