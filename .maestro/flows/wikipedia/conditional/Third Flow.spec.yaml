appId: ${appId}
tags:
  - smokeTest
---
- launchApp:
    clearState: true

- runScript: 
    file: "../../../src/pages/LaunchStepper.page.js"

- runFlow:
    when:
      visible:
        id: ${output.launchStepper.skipBtn}
    commands:
        - assertVisible:
            id: "invalid_id"