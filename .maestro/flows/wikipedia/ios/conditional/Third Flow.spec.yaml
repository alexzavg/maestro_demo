appId: ${appId}
tags:
  - smokeTest
---
- launchApp:
    clearState: true

- runScript:
    file: "../../../../src/pages/ios/LaunchStepper.page.js"

- runFlow:
    when:
      visible:
        text: ${output.launchStepper.skipBtn}
    commands:
        - assertVisible:
            id: "invalid_id"
